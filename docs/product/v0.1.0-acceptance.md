# v0.1.0 acceptance evidence

Status: **PENDING — NO-GO; not release-qualified**

This document records implementation-head evidence for the M0 Control Plane and fake-Connector baseline. It must not be used to claim real Harness, physical-glasses, later-milestone reliability/operations, backup/restore, or RTC completion.

Status legend: **PENDING** means evidence has not yet been recorded or the required final-head rerun is outstanding; **PASS/FAIL** is the observed result of an executed gate; **BLOCKED-ENVIRONMENT** means the named environment cannot run the gate; **NOT YET** means the requirement belongs to a later milestone; **PARTIAL-PROXY** means only a bounded substitute is covered. Local OCI unavailability is **BLOCKED-ENVIRONMENT** for local execution. The authoritative GitHub runtime result below is **PASS only for the tested implementation SHA**; fresh Governance and Runtime checks for the final evidence-only head remain **PENDING**.

## Evidence identity

| Field | Value |
|---|---|
| Release candidate | `v0.1.0` |
| Base SHA | `088b847cd5d6d5628b0a5c321b186428d30b3edd` |
| Tested Task 7 implementation SHA | `898503c799ee0e7214e4f8eeacdcfed100f5b02f` |
| Final evidence-only head | **PENDING at this pre-push stage.** A commit cannot embed its own SHA without self-reference. Only after independent review, integration, and push will [PR #45](https://github.com/EthanSMC/qwen-harness-bridge/pull/45)'s remote head and Git history supply the immutable external identity; the controller must verify that remote identity after push. Fresh final-head checks remain **PENDING**. |
| GitHub runtime run | [Run 33838960808](https://github.com/EthanSMC/qwen-harness-bridge/actions/runs/33838960808); exact head `898503c799ee0e7214e4f8eeacdcfed100f5b02f`; conclusion `success` |
| Run start/end (UTC) | `2026-09-04T05:01:38Z` / `2026-09-04T05:03:36Z` |
| Runtime job | Start/end `2026-09-04T05:01:42Z` / `2026-09-04T05:03:35Z`; GitHub Actions runner `1000000561`, label `ubuntu-latest` |
| Build and start step | `success`; `2026-09-04T05:01:47Z` to `2026-09-04T05:03:25Z` (`98 seconds`) |
| Operator/host | GitHub-hosted runner `1000000561` / `ubuntu-latest`; no local host path recorded |
| Runtime workdir | `/app` (container path observed in artifact) |
| Node / pnpm | Node `v22.23.2`; pnpm `10.15.1` |
| Docker / Compose | Docker `28.0.4`; Docker Compose `2.38.2` |
| PostgreSQL | Exact patch version not captured; source configuration pin is recorded separately below |
| Migration observed | Table `__drizzle_migrations`; tag `0002_result_acknowledgement`; `when` `1788244364352`; SHA-256 `2e4a1f323453e6f4a7ec7319250f474ce7552c536ed3a55af4b5fe52c5a9cb89` |
| OCI image digest observed | `sha256:ae5f2a44855452392fc78ee5fc2bd0406937c2741097af0b7ab1f9aa6ca4eeed` |
| Configuration digest observed | `sha256:cda959c664213f73c21a1f9ff82f42c09aa2ba27cc956198b1f998cd362a4a27`; digest only, never configuration values |
| Runtime observation timestamp | `observed_at_utc=2026-09-04T05:03:32Z` |
| Artifact | [Artifact 9924276758, `runtime-evidence`](https://github.com/EthanSMC/qwen-harness-bridge/actions/runs/33838960808/artifacts/9924276758); created `2026-09-04T05:03:33Z`; expires `2026-12-03T05:01:39Z`; downloaded file SHA-256 `9e884d75071e2bc7f104ce4f669a0e8d79de8085dd64e53a75e97178448f8522` |
| RTC | Disabled; not part of `v0.1.0` |

## Issue #1 legacy workspace/protocol baseline reconciliation

This section is legacy Issue #1 closure evidence. It records the current workspace and protocol baseline at `69f433290e4435127f19c282511e9eacd620d41d` and does not claim that the legacy issue is merged, closed, or release-qualified.

### Workspace manifests

| Manifest | Current declaration |
|---|---|
| `pnpm-workspace.yaml` | `apps/*` and `packages/*`; `injectWorkspacePackages: true`; injected dependencies resync after `build` |
| Root `package.json` | private package `qwen-harness-bridge`, version `0.1.0`, pnpm `10.15.1`, Node `^20.19.0 || >=22.12.0`; recursive `build`/`typecheck`, root `check`, and Vitest `test` scripts |
| `packages/protocol/package.json` | private ESM package `@qhb/protocol` `0.1.0`; publishes only `dist`; `.` exports `dist/index.d.ts` for types and `dist/index.js` as default; build/typecheck scripts present |
| `apps/control-plane/package.json` | private ESM package `@qhb/control-plane` `0.1.0`; publishes only `dist`; consumes `@qhb/protocol` through `workspace:*`; build/typecheck scripts present |

### Protocol schemas and exports

`packages/protocol/src/index.ts` re-exports `connector.ts`, `job.ts`, and `mcp.ts`. The public baseline is:

| Source | Public schemas / exports |
|---|---|
| `job.ts` | `JobStatusSchema`, `ConnectorHealthSchema`, `RepositoryIdSchema`, `ShortJobIdSchema`, `JobSummarySchema`, `SubmitTaskInputSchema`, `JobReceiptSchema`; inferred types `JobStatus`, `ConnectorHealth`, `JobSummary`, `SubmitTaskInput`, `JobReceipt` |
| `mcp.ts` | `ListTasksInputSchema`, `GetTaskInputSchema`, `CancelTaskInputSchema`, `ListPendingApprovalsInputSchema`, `DecideApprovalInputSchema`, `GetTaskResultInputSchema`; corresponding six inferred input types |
| `connector.ts` | Twelve public payload schemas from hello/heartbeat, claim/event, approval/cancellation, welcome/offer/cancel, decision, acknowledgement, and protocol-error messages; public unions `ConnectorClientMessageSchema`, `ConnectorServerMessageSchema`, `EnvelopeSchema`, `ConnectorEnvelopeSchema`; `SequenceCursor`, `SequenceCursorResult`, `rfc3339InstantKey`, two sequence error constants, and the exported payload/message types |

The historical origin of this baseline is [`35005c499cb6552424fd745233825a260c214a3e`](https://github.com/EthanSMC/qwen-harness-bridge/commit/35005c499cb6552424fd745233825a260c214a3e) (`feat(protocol): define job and mcp contracts`). A test-first RED observation and the claim that the original change was minimal are historical-process claims: they may be established only by authentic historical logs/commits, are not recreated by this document, and must not be fabricated from the current green result or file list.

### Focused verification at the reconciliation base

| Gate | Command | Observed result |
|---|---|---|
| Protocol tests | `pnpm exec vitest run --config /dev/null packages/protocol/src/connector.test.ts packages/protocol/src/job.test.ts` | **PASS** — 2 files, 31 tests |
| Workspace typecheck | `pnpm typecheck` | **PASS** — `@qhb/protocol` and `@qhb/control-plane` typecheck completed |
| Workspace build | `pnpm build` | **PASS** — protocol and control-plane builds completed |

This PR is the legacy Issue #1 closure-evidence PR. PR number/URL, pushed head, required checks, merge commit, and GitHub auto-close confirmation are **PENDING** until those events occur. The v0.1.0 decision therefore remains **PENDING / NO-GO** until Issue #1 is merged and auto-closed and the M0 release gate passes; this section does not authorize a tag or release.

## Observed artifact values

The following values are copied from the linked `runtime-evidence` artifact generated by the successful implementation-head run. They are runtime observations, not source declarations.

### Health, metrics, privacy, and recovery

| Observation | Exact result |
|---|---|
| `/health/live` | `live_status=200`; `live_body={"status":"ok"}` |
| `/health/ready` | `ready_status=200`; `ready_body={"status":"ready"}` |
| `/metrics` | `metrics_status=200`; `metrics_content_type=text/plain; version=0.0.4; charset=utf-8` |
| Metrics privacy audit | `privacy=passed` |
| Database-loss `/health/ready` | `database_loss_readiness_status=503`; `database_loss_readiness_body={"status":"not_ready"}`; `database_loss_readiness_content_type=application/json; charset=utf-8` |
| Database-loss privacy audit | `db_loss_privacy=passed` |
| Database-loss `/health/live` | `db_loss_live_status=200`; `db_loss_live_body={"status":"ok"}` |
| Recovery `/health/ready` | `recovery_readiness_status=200`; `recovery_ready_body={"status":"ready"}` |

The metrics body exposed the bounded metric families `qhb_mcp_submit_duration_seconds`, `qhb_connector_online`, `qhb_job_queue_age_seconds`, `qhb_jobs`, `qhb_connector_messages_total`, and `qhb_errors_total`. All observed values were `0`; the submit histogram had `count 0` and `sum 0`, and its bucket counts were `0`. The status, message-type, and error-code labels were bounded as emitted by the artifact. Therefore, zero runtime submit samples were captured: submit latency sample count and p50/p95/p99/max remain **PENDING**, and this run is not submit-latency evidence.

### Build, tests, and runtime integrity

| Observation | Exact result |
|---|---|
| Repository gate | `repository_test_result=passed`; `repository_test_count=381` |
| Runtime-focused gate | `focused_test_result=passed`; `focused_test_count=96` |
| Migration identity | `migration_table=__drizzle_migrations`; `migration_tag=0002_result_acknowledgement`; `migration_when=1788244364352`; `migration_sha256=2e4a1f323453e6f4a7ec7319250f474ce7552c536ed3a55af4b5fe52c5a9cb89` |
| Effective runtime image | `image_digest=sha256:ae5f2a44855452392fc78ee5fc2bd0406937c2741097af0b7ab1f9aa6ca4eeed` |
| Effective Compose configuration | `config_digest=sha256:cda959c664213f73c21a1f9ff82f42c09aa2ba27cc956198b1f998cd362a4a27`; no resolved values recorded |
| Runtime cleanup | Cleanup step `success`; the runtime job cleanup completed without recording secret values or local host paths |

The artifact records `tested_commit=898503c799ee0e7214e4f8eeacdcfed100f5b02f` and `runtime_workdir=/app`. It contains no secret values or local development-host path. The run URL contains repository identity for provenance, and `/app` is a non-secret container absolute path; neither is treated as a privacy failure. The run URL and artifact link are intentionally retained for provenance.

Run conclusion, run/job timestamps, runner identity, build/start timing, and cleanup-step success above are additionally sourced from the controller’s GitHub API query; endpoint, digest, migration, version, test-count, and privacy values are copied from the linked artifact.

## Source configuration

These are source declarations at the tested implementation SHA, not claims that a PostgreSQL patch version was separately observed:

| Source | Exact declaration |
|---|---|
| [`docker-compose.yml`](../../docker-compose.yml), `postgres` service | `image: postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685` |

The configured PostgreSQL image is `postgres:16-alpine` with the source pin above; no PostgreSQL patch version is inferred. Runtime migration/query behavior passed and yielded the observed migration row and readiness/recovery results above. The runtime `image_digest` is the effective control-plane image digest, and `config_digest` is the digest-only effective Compose configuration observation. Neither replaces the source pin above.

## Evidence procedure

1. Commit the implementation and this evidence schema.
2. Run local application gates and independent review.
3. Push the Issue #7 branch and open a PR containing `Closes #7`.
4. Run the dedicated GitHub Docker gate against the exact implementation SHA.
5. Add an evidence-only commit containing the exact run URL and observations below.
6. Rerun all current-head gates and obtain fresh independent review before merge.

This two-phase procedure avoids a self-referential commit hash: the implementation SHA is immutable evidence input, while the later evidence-only commit records its results. The final evidence-only commit's external identity is **PENDING** at this pre-push stage because a commit cannot embed its own SHA without self-reference. Only after independent review, integration, and push will PR #45's remote head and Git history supply the immutable external identity; the controller must verify that remote identity after push.

## Commands and current state

| Gate | Exact command or source | Current status | Evidence / limitation |
|---|---|---|---|
| Repository check | `pnpm check` | **PASS** on implementation SHA | GitHub runtime artifact records 381 repository tests passed. |
| Build | `pnpm build` | **PASS** on implementation SHA | Build-and-start step succeeded in 98 seconds; fresh final-head rerun remains PENDING. |
| Health/metrics contract | Runtime workflow focused gate | **PASS** within the 96-test focused gate | Artifact records 96 focused tests passed and exact endpoint observations above. |
| Runtime build contract | Runtime workflow focused gate | **PASS** within the 96-test focused gate | Static/build contract evidence is complemented by the successful OCI runtime run; fresh final-head rerun remains PENDING. |
| Foundation fake flow | Four-flow aggregate | **PENDING** | The artifact does not contain explicit four-flow aggregate evidence or runtime submit samples; do not infer it from the 96 focused tests. |
| OCI runtime | GitHub runtime workflow | **PASS** for implementation SHA / **PENDING** for final evidence-only head | [Run 33838960808](https://github.com/EthanSMC/qwen-harness-bridge/actions/runs/33838960808) succeeded at the exact implementation SHA. Local OCI remains **BLOCKED-ENVIRONMENT** because Docker-compatible runtime is unavailable on the development host. |
| Governance | GitHub required `governance` check | **PASS** for implementation head / **PENDING** final evidence-head rerun | [Governance run 33839001067](https://github.com/EthanSMC/qwen-harness-bridge/actions/runs/33839001067) succeeded on the implementation head. Fresh Governance and Runtime checks must run after this evidence-only commit. |

## Spec section 16 matrix

| Requirement | Status | Evidence / limitation |
|---|---|---|
| 16.1 state, idempotency, leases, approval, cancellation, terminal immutability | **PENDING revalidation** | The artifact does not provide an explicit four-flow aggregate or runtime flow samples. Existing focused suites are not converted into a broader runtime claim here. |
| 16.1 redaction and bounded public presentation | **PARTIAL-PROXY** | The implementation-head metrics and database-loss response privacy audits passed; this does not establish all presenter/protocol behavior. |
| 16.1 canonical local path and symlink containment | **NOT YET — M1/v0.2.0** | Requires the real local Harness policy boundary. |
| 16.1 localized glasses status/UI | **NOT YET — M2/v0.3.0** | No device UI exists in M0. |
| 16.2 seven MCP tools and Connector schemas/version rejection | **PENDING revalidation** | No explicit aggregate evidence for this requirement is present in the runtime artifact. |
| 16.2 submit latency | **PENDING** | The runtime submit histogram has zero samples; p50/p95/p99/max and threshold remain unrecorded. This is not glasses end-to-end latency. |
| 16.3 PostgreSQL transactions/revisions | **PENDING revalidation** | Runtime migration identity and readiness evidence passed, but this artifact is not a complete transaction/revision test report. |
| 16.3 fake Connector lifecycle, reconnect, duplicates, approval, cancellation, result acknowledgement | **PENDING** | No explicit four-flow aggregate is present in the artifact. Do not infer these flows from the 96 focused tests. |
| 16.3 Control Plane process restart and partitions | **NOT YET — M3/v0.4.0** | Health failure separation is only a baseline proxy. |
| 16.4 credential/replay/redaction baseline | **PARTIAL-PROXY** | The recorded runtime privacy audits passed for metrics and database-loss readiness output; revocation and rate limits remain M3. |
| 16.4 canonical repository/path enforcement | **NOT YET — M1/v0.2.0** | Cloud input validation is not local filesystem containment. |
| 16.4 rate limits | **NOT YET — M3/v0.4.0** | No M0 claim. |
| 16.5 real Harness and physical-glasses E2E | **NOT YET — M1/M2** | The runtime artifact contains no real Harness or physical-glasses evidence. |
| 16.5 RTC cases | **NOT YET — experimental v0.5.0** | RTC is disabled in stable M0. |
| OCI build, migration-safe startup, health/readiness, metrics | **PASS** for implementation SHA / **PENDING** final evidence-only head | GitHub runtime run succeeded; exact endpoint responses, migration row, image/config digests, privacy audits, and cleanup are recorded above. A fresh exact-final-head runtime gate is still required. |
| Backup/restore and rollback rehearsal | **NOT YET — v1.0.0 gate** | No M0 claim. |

## Runtime observations

All observations in this section are from the exact implementation-head [GitHub runtime run](https://github.com/EthanSMC/qwen-harness-bridge/actions/runs/33838960808) and its [runtime-evidence artifact](https://github.com/EthanSMC/qwen-harness-bridge/actions/runs/33838960808/artifacts/9924276758):

- Build and start: **PASS**, `2026-09-04T05:01:47Z` to `2026-09-04T05:03:25Z`, 98 seconds.
- Local OCI execution: **BLOCKED-ENVIRONMENT** — Docker-compatible runtime unavailable on the development host; this is not authoritative runtime evidence.
- Authoritative GitHub OCI execution: **PASS** for `898503c799ee0e7214e4f8eeacdcfed100f5b02f`; final evidence-only head: **PENDING** rerun.
- Runtime job: `2026-09-04T05:01:42Z` to `2026-09-04T05:03:35Z`, runner `1000000561`, `ubuntu-latest`.
- Compose services and health states: startup and health gate **PASS**; `/health/live` and `/health/ready` returned the exact 200 responses recorded above.
- `/health/live`: HTTP `200`, `{"status":"ok"}`.
- `/health/ready`: HTTP `200`, `{"status":"ready"}`.
- `/metrics`: HTTP `200`, `text/plain; version=0.0.4; charset=utf-8`; required metric families and bounded labels were observed; privacy audit `passed`.
- PostgreSQL-stop readiness: HTTP `503`, `{"status":"not_ready"}`, `application/json; charset=utf-8`; database-loss privacy audit `passed`.
- PostgreSQL-stop liveness: HTTP `200`, `{"status":"ok"}`.
- Recovery readiness: HTTP `200`, `{"status":"ready"}`.
- Migration row: `__drizzle_migrations`, `0002_result_acknowledgement`, `1788244364352`, SHA-256 `2e4a1f323453e6f4a7ec7319250f474ce7552c536ed3a55af4b5fe52c5a9cb89`.
- Image digest: `sha256:ae5f2a44855452392fc78ee5fc2bd0406937c2741097af0b7ab1f9aa6ca4eeed`.
- Configuration digest: `sha256:cda959c664213f73c21a1f9ff82f42c09aa2ba27cc956198b1f998cd362a4a27`.
- Cleanup step result: **PASS**; no secret values or local development-host paths are recorded here. The artifact’s non-secret `run_url` repository identity and `runtime_workdir=/app` container absolute path remain explicit provenance/runtime-path exceptions.

## Flow observations

- Submit latency sample count / p50 / p95 / p99 / max: **PENDING**; the runtime histogram count and sum were both `0`.
- Submit → claim → progress: **PENDING**; no explicit runtime flow sample is present.
- Reconnect and exact duplicate handling: **PENDING**; no explicit runtime flow sample is present.
- Approval request, exact decision binding, and resume: **PENDING**; no explicit runtime flow sample is present.
- Cancellation arbitration and one immutable terminal winner: **PENDING**; no explicit runtime flow sample is present.
- Terminal result and one database acknowledgement timestamp: **PENDING**; migration evidence is present, but no explicit runtime result-flow sample is present.
- Explicit foundation/approval/cancellation/result aggregate: **PENDING**; not present in this artifact. The histogram count and sum are both `0`.
- Evidence artifact privacy/cleanliness beyond the scoped audits: **PARTIAL-PROXY**, not a broad PASS. The artifact retains the non-secret `run_url` repository identity and `runtime_workdir=/app` container absolute path; no secret values or local development-host path were recorded.

## Release decision

`v0.1.0` remains **PENDING / NO-GO**. [Issue #7](https://github.com/EthanSMC/qwen-harness-bridge/issues/7) is not merged or closed. The final evidence-only head checks have not run: fresh Governance and Runtime checks are required after this commit. Legacy milestone [Issue #1](https://github.com/EthanSMC/qwen-harness-bridge/issues/1) and the M0 release gate remain unresolved. Every M0 Issue must be closed or formally moved with evidence, the exact final-head Governance and runtime gates must pass, and release tag/GitHub Release evidence must be recorded before release qualification.
