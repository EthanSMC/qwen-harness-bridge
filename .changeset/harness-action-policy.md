---
"@qhb/harness-plugin": minor
---

Add canonical repository action policy with registration-bound trusted executable identity and fail-closed action guards. Resolve executable paths against immutable trusted registration, reject repository traversal and symlink escapes, and require approval for package installation, pushes, and deployment.

Bind policy setup to the official bridge-owned Agent scope and require private one-use proof of audited allowed-once approval at the final guard. Parse supported command argument roles, protect credential resources without blocking ordinary security source patterns, and allow safe bounded test/build outputs while requiring approval for compiler clean mode. Add the exact official scope peer used to validate registration.
