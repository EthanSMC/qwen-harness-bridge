---
"@qhb/harness-plugin": patch
---

Add an opt-in shared owned-terminal transaction linking local outcome identity,
journal closure, mapping, outbox, and receipt profile. Reconcile remote terminal
state without fabricating outbound events, and reject generic owned-terminal
publication. Native plugin activation remains gated on its separate integration.
